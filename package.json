{
  "name": "@coffeemakerstudio/bean",
  "version": "0.1.0",
  "description": "Reusable generic engine system and physics capability contracts. Bean depends only on Roast.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json && bun build src/index.ts --outdir dist --format esm",
    "test": "bun test tests/*.test.ts"
  },
  "peerDependencies": {
    "@coffeemakerstudio/roast": "^0.1.0"
  },
  "peerDependenciesMeta": {
    "@coffeemakerstudio/roast": {
      "optional": true
    }
  }
}
